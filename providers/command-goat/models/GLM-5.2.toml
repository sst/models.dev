# Sources (accessed 2026-09-09):
# - Pricing and model details: GOAT plan (https://commandcode.ai/docs/plans/goat)
# - Plan scope: GOAT only; Go/Pro/Max plans excluded (plan-specific provider, cf. alibaba-coding-plan)
# - Reasoning: baseline per providers/zhipuai/models/glm-5.2.toml; effort=["high","max"] (gateway verified live 2026-09-11: 8 models/42 calls; none|minimal rejected by enum, enable_thinking ignored; toggle/budget omitted).
base_model = "zhipuai/glm-5.2"
reasoning_options = [{ type = "effort", values = ["high","max"] }]

[cost]
input = 1.4
output = 4.4
cache_read = 0.26
