# Sources (accessed 2026-09-09):
# - Pricing and model details: GOAT plan (https://commandcode.ai/docs/plans/goat)
# - Plan scope: GOAT only; Go/Pro/Max plans excluded (plan-specific provider, cf. alibaba-coding-plan)
# - Reasoning: baseline per providers/stepfun-ai/models/step-3.7-flash.toml; effort=["low","medium","high"] (gateway verified live 2026-09-11: 8 models/42 calls; none|minimal rejected by enum, enable_thinking ignored; toggle/budget omitted).
base_model = "stepfun/step-3.7-flash"
reasoning_options = [{ type = "effort", values = ["low","medium","high"] }]

[cost]
input = 0.2
output = 1.15
cache_read = 0.04
