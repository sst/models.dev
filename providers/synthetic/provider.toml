# Catalog metadata (authenticated 2026-08-27):
# https://api.synthetic.new/openai/v1/models
# https://dev.synthetic.new/docs/api/models
# Reasoning request/response fields:
# https://dev.synthetic.new/docs/openai/chat-completions
# The catalog's per-model `reasoning_parameters.efforts` values are sent as the
# top-level `reasoning_effort` request field. Prices are USD per million tokens.

name = "Synthetic"
env = ["SYNTHETIC_API_KEY"]
npm = "@ai-sdk/openai-compatible"
doc = "https://synthetic.new/pricing"
api = "https://api.synthetic.new/openai/v1"
